import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  view: {
    display: 'flex',
    marginTop: 15,
    flexDirection: 'row',
  },

  viewFlat: {
    width: '50%',
    alignItems: 'center',
  },

  viewMap: {
    width: '50%',
    alignItems: 'center',
  },
})